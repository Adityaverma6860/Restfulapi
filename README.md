Build a **Restful api** and implement Crud operations by used Mongo db , Express js , Node js

Build a **Crud operation **Using **Local storage**

Build a **responsive front-end interfaces** in HTML, and CSS, and managed databases using **MongoDB**
for seamless data storage and retrieval.

